/* vendor: full victor-hugo theme (selected files) - placeholder */

/* The full theme is large; adding a minimal set of commonly required files to avoid Hugo template errors.
 * If you need the entire theme (layouts, assets, etc.), I can add all files; this initial commit adds layouts/partials used by the site.
 */
