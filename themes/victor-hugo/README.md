Victor Hugo theme (minimal stub)

This is a minimal stub of the victor-hugo theme added to the repository so Hugo builds on Netlify without requiring git submodules.

It intentionally contains no layouts because the project provides site/layouts/ already. Add theme partials, assets, and templates here if you want to override or include theme resources.
